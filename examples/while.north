proc main do
	0 value dup variable !

	while value ? 10 < do
		if value ? 4 > do
			1 while dup 15 < do
				if dup 6 >= do
					break
				end
				dup print
				1 +
			end
			drop
		end

		"VALUE" print
		value ? print
		value ? 1 + value !
	end
end
